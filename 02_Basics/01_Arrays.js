// [] brackets, {} Braces, () Parenthesis
//arrays in js are resizable and mixed data types, cannot acces like arr["ved"]
//push,pop (for insert and remove from end)
//unshift(add at start),shit(remove firststarting element) but takes time bcz all index need to shift and unshift
//slice  vs splice 
//slice arr.slice(0,3) -> gives values from arr[0],1,2, dosent modifies the original array
//splice arr.splice(0,3) -> returns and removes values arr[0],1,2,arr[3] from original array 

//includes, indexOf, join (return a string with all values of array separated by commas)
